name: Update EPG

on:
  schedule:
    - cron: '0 3 * * *'  # Щодня о 03:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-epg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies and update EPG
        run: |
          pip install requests
          python update_epg.py

      - name: Set up Git config and checkout branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B epg-bot

      - name: Commit changes if any
        run: |
          git add epg2.xml
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "Автоматическое обновление EPG"

      - name: Push to epg-bot branch
        run: git push origin epg-bot --force

      - name: Create or update Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Автоматическое обновление EPG
          branch: epg-bot
          title: Обновление EPG
          body: |
            Этот pull request создан автоматически GitHub Actions и содержит обновлённый файл `epg2.xml`.
            Проверь изменения и подтверди слияние, если всё в порядке.
          base: main
